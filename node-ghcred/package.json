{
  "name": "node-ghcred",
  "version": "1.0.0",
  "description": "GitHub OAuth token exchange proxy — keeps the client secret off the browser",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
